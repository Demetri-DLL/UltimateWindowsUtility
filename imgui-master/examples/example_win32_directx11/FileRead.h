#pragma once
#include <iostream>
#include <fstream>
#include <string>

class FileRead {
public:
    void WriteFileData(std::time_t Output, const char* FileName);
    std::string FileRead::ReadFileData(int No);
private:

};
